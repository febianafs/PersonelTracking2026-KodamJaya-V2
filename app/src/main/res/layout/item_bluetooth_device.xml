<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardBackgroundColor="@color/background2"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="10dp">

        <!-- Icon device -->
        <androidx.cardview.widget.CardView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginEnd="12dp"
            app:cardBackgroundColor="#1A3A5C"
            app:cardCornerRadius="8dp"
            app:cardElevation="0dp">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_watch"
                app:tint="@color/primary" />

        </androidx.cardview.widget.CardView>

        <!-- Nama & MAC -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvDeviceName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Device Name"
                android:textColor="@color/white"
                android:textSize="14sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvDeviceMac"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="AA:BB:CC:DD:EE:FF"
                android:textColor="@color/gray"
                android:textSize="11sp" />

        </LinearLayout>

        <!-- RSSI -->
        <TextView
            android:id="@+id/tvRssi"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:text="-62 dBm"
            android:textColor="@color/gray"
            android:textSize="11sp" />

        <!-- Tombol aksi: Hubungkan / Menghubungkan... / Terhubung+Lepaskan -->
        <LinearLayout
            android:id="@+id/layoutActions"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Tombol Hubungkan -->
            <TextView
                android:id="@+id/btnConnect"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_btn_connect"
                android:paddingHorizontal="10dp"
                android:paddingVertical="5dp"
                android:text="Connect"
                android:textColor="@color/primary"
                android:textSize="11sp"
                android:visibility="gone" />

            <!-- Tombol Menghubungkan (loading) -->
            <TextView
                android:id="@+id/btnConnecting"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_btn_connecting"
                android:paddingHorizontal="10dp"
                android:paddingVertical="5dp"
                android:text="Connecting..."
                android:textColor="@color/orange"
                android:textSize="11sp"
                android:visibility="gone" />

            <!-- Badge Terhubung -->
            <TextView
                android:id="@+id/badgeConnected"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="6dp"
                android:background="@drawable/bg_badge_connected"
                android:paddingHorizontal="8dp"
                android:paddingVertical="5dp"
                android:text="Connected"
                android:textColor="@color/primary"
                android:textSize="11sp"
                android:visibility="gone" />

            <!-- Tombol Lepaskan -->
            <TextView
                android:id="@+id/btnDisconnect"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_btn_disconnect"
                android:paddingHorizontal="10dp"
                android:paddingVertical="5dp"
                android:text="Disconnect"
                android:textColor="@color/red"
                android:textSize="11sp"
                android:visibility="gone" />

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>